{
	email {$ACME_EMAIL}
}

{$DOMAIN} {
	encode gzip zstd

	basic_auth {
		{$BASIC_AUTH_USER} {$BASIC_AUTH_HASH}
	}

	handle /stream-api/* {
		uri strip_prefix /stream-api
		reverse_proxy stream-server:8080
	}

	handle {
		reverse_proxy frontend:80
	}
}
